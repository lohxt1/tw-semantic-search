export const HOME_HOSTNAMES = new Set(["localhost:3000"]);

export const PROTECTED_PAGE_ROUTES = new Set(["search"]);

// API paths
export const SEARCHPATH = "/api/search";
export const HISTORYPATH = "/api/history";
